// GHOST FIVE // SPECTRAL CORE // VECTIS
// Canonical language example: booleans.
mission "Boolean literals" {
    when true {
        publish result;
    } otherwise {
        request human_reviewer;
    }
}
