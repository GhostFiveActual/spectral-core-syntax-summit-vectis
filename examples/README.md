# VECTIS Official Examples

Files under `valid/` are normative parser-conformance examples and must parse
successfully.

Files under `invalid/` are normative parser-diagnostic examples and must be
rejected by the parser.

The authoritative concrete syntax is defined by
`docs/spec/grammar.md`.
